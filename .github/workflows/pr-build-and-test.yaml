name: pr-build-and-test

on:
  pull_request:

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet clean Icod.CommandFramework.sln -c Debug
      - run: dotnet restore Icod.CommandFramework.sln
      - run: dotnet build Icod.CommandFramework.sln -c Debug --no-restore
      - run: dotnet test Icod.CommandFramework.sln -c Debug --no-build --logger trx